# 历史遗留文件引用边界

## 一、历史遗留文件

| 文件 | 引用场景 | 引用者 |
|---|---|---|
| `.init-checkpoint.yaml` | 仅状态迁移专项核查 | `mes-verify-state-migration` |
| `.sync-record.json` | 仅状态迁移专项核查 | `mes-verify-state-migration` |

---

## 二、禁止行为

- ❌ 新代码直接读取历史遗留文件
- ❌ 将历史遗留文件作为主读取路径
- ❌ 在新规则中将历史遗留文件描述为正式兼容视图

---

## 三、使用边界

历史遗留文件仅用于专项核查，不参与新的正式消费链路。
