# 数据一致性测试

## 数据一致性测试

### 测试场景：[数据操作]

**事务范围**：
- 涉及服务：[服务列表]
- 涉及数据库表：[表列表]

**测试步骤**：
1. 执行正常流程，验证数据一致性
2. 模拟服务B失败，验证事务回滚
3. 模拟网络超时，验证最终一致性

**验证SQL**：
\`\`\`sql
-- 验证主表数据
SELECT * FROM table_a WHERE id = ?;

-- 验证关联表数据
SELECT * FROM table_b WHERE a_id = ?;
\`\`\`
```

### 步骤4：设计异常场景测试

```markdown
