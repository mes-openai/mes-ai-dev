# mes-develop-backend-model / graph-minimal-field-scope 资源说明

## 场景类型
- 图谱边界场景

## 模拟输入
- graphify 提示字段可以合并重命名，但当前任务只允许补齐缺失字段。

## 观察点
- 是否仅接受图谱作为导读
- 是否坚持最小字段修改
- 是否拒绝无关重命名、无关格式化与顺手重构

## 期望检查动作
- 先确认设计结论，再决定字段调整范围
- 只做已确认契约需要的最小修改

## 期望结论
- 仅补齐目标字段，不扩展模型职责。

## 回归判定提示
- 若把字段合并或模型重命名当作当前任务内容，则判定失败。
