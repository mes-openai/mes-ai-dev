# 图谱候选链路不替代服务链冻结 eval 资源说明

本 eval 验证 `mes-design-service-chain` 在多服务 / 多 Provider 场景下的门禁行为。

- GitNexus：仅用于候选链路发现和逐段校验。
- graphify：仅用于服务关系导读。
- 正式结论：最终服务链、Provider 选择理由、禁止路径、失败路径、事务边界和契约边界必须写入 `design.md`。
- 拦截重点：不得把候选链路直接冻结为服务链，不得新增未经确认的中间编排层、聚合服务或平行调用机制。
