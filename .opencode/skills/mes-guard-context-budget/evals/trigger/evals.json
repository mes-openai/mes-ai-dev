{
  "skill": "mes-guard-context-budget",
  "version": "1.0.0",
  "samples": [
    {
      "id": "trigger-001",
      "input": "Guard context budget per phase to prevent overflow. Trigger: budget check, context guard, token limit",
      "expected": "mes-guard-context-budget",
      "notes": "基础触发样例，后续可按真实场景扩充。"
    }
  ]
}
