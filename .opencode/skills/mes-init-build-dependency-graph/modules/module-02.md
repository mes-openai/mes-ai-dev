# 何时使用

## 何时使用
- 在init-extract-api、mes-init-analyze-config、init-index-database完成后执行
- 在 `/mes-init-converge` 中作为全局 registry / dependency 汇总步骤执行
- 需要完整的依赖关系图时
- 触发关键词：dependency graph, service dependencies, build dependencies
