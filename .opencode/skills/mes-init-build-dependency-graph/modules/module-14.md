# 约束规则

## 约束规则
1. **执行前必须先输出计划**，初始化阶段无需等待用户确认
2. 必须读取所有相关依赖文件
3. 依赖关系必须双向验证
4. 循环依赖必须检测和标注
5. 未使用API必须列出
6. 风险点必须明确标注
7. 所有输出使用中文
8. **共享服务必须识别和标注**
9. **必须统计每个服务的消费者数量**
10. **必须生成影响传导关系图**
11. **高影响服务必须标注变更通知要求**
