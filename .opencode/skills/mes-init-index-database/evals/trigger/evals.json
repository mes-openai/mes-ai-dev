{
  "skill": "mes-init-index-database",
  "version": "1.0.0",
  "samples": [
    {
      "id": "trigger-001",
      "input": "Parse database scripts to build table structure index. Trigger keywords: index database, table structure, schema index, database registry.",
      "expected": "mes-init-index-database",
      "notes": "基础触发样例，后续可按真实场景扩充。"
    }
  ]
}
