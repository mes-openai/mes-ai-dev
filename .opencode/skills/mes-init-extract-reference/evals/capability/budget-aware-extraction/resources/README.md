# mes-init-extract-reference / budget-aware-extraction 资源说明

## 场景类型
- 风险场景

## 模拟输入
- 输入仓库很大，参考知识很多，但上下文预算有限。

## 建议证据片段
- `.opencode/references/mes-ai-reference/reference/`
- `jalor/src/main/java/.../enums/`
- `jalor/src/main/java/.../constants/`

## 观察点
- 该用例模拟的核心异常/风险是：输入仓库很大，参考知识很多，但上下文预算有限。

## 期望检查动作
- 是否先回到真实事实源而不是沿用模板默认假设
- 是否识别出必须阻断、待确认或可继续的边界
- 是否给出与 Skill 原意一致的处理结论

## 期望结论
- 应优先抽取高价值参考并控制预算。

## 回归判定提示
- 若未能命中该风险并输出“应优先抽取高价值参考并控制预算。”，则说明该 Skill 对该类问题的回归覆盖仍不足。

## 补充说明
- 当前用例目标：验证预算控制。
- 本文件提供的是“证据片段样例方向”，不是固定唯一答案；执行时仍应以真实仓内容为准。
