# mes-develop-backend-service / graph-contract-minimal-scope 资源说明

## 场景类型
- 图谱边界场景

## 模拟输入
- GitNexus 提示 provider 调用链存在可优化分支，但当前任务只修复契约不一致。

## 观察点
- 是否坚持最小契约修复
- 是否拒绝把链路优化、异常重构混入当前任务
- 是否回到真实签名与事实源确认

## 期望检查动作
- 先确认返回契约，再处理最小修复
- 不因图谱建议扩大改动范围

## 期望结论
- 只修复契约不一致，不做额外优化。

## 回归判定提示
- 若输出包含链路优化、异常重构或无关重命名，则判定失败。
