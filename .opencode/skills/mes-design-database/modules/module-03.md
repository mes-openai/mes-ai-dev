# 表真实性优先要求

## 表真实性优先要求

- 设计文档中的每一张表都必须先绑定真实事实来源，再进入表结构设计。
- 真实事实来源仅允许来自以下几类：真实 DDL 脚本、`database-index` 索引、`database-registry.md`、现有 Entity / Mapper / SQL、已明确批准的新表结论。
- 不得因为需求中提到某个业务概念，就直接脑补一张新表。
- 若某张表既不在真实数据库对象中，也没有“明确新增表”的批准依据，必须阻断设计，不得写入 `database-design.md`。
