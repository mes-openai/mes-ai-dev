# 约束规则

## 约束规则

### 必须遵守

1. **执行前输出计划**
2. **覆盖全部核心业务动作** — business-flows.md 中的每条主链路必须追踪
3. **诚实标记** — 找不到就标❌，推断的就标⚠️，不美化结果
4. **上下文管理** — 每个链路最多读取5个源文件，调用 `mes-guard-context-budget`

### 禁止行为

1. ❌ 不修改已有知识库文件
2. ❌ 不跳过任何链路
3. ❌ 不把推断结果标为✅
