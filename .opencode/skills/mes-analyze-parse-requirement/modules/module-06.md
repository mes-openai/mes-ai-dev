# 输出

## 输出
生成文件：`mes-ai-dev/workspace/requirements/{REQ-ID}/raw-requirement.md`

条件输出：
- `mes-ai-dev/workspace/requirements/{REQ-ID}/exploration.md`：需求歧义大、多方案、多仓/跨服务边界不清、存量结构适配风险高时生成
- `mes-ai-dev/workspace/requirements/{REQ-ID}/proposal.md`：需要用户确认方案取舍、仓落点、provider、复用路径或高风险前置决策时生成

文件结构：
```markdown
# 原始需求解析
