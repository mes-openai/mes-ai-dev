# 输入依赖

## 输入依赖

| 输入项 | 路径 | 说明 |
|--------|------|------|
| change-list | `mes-ai-dev/workspace/refresh/change-list.md` | 变更文件清单 |
| api-registry | `mes-ai-dev/knowledge/dependency-graph/api-registry.md` | API注册表 |

> 本 Skill 仅在知识库**已收拢的全局视角**下运行：允许直接读取全局 `api-registry.md`，不适用于初始化局部片段阶段。
