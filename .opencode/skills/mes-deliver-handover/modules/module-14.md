# 六、签署确认

## 六、签署确认
- 开发方确认
- 测试方确认
- 运维方确认
```

3. 写入 `workspace/delivery/REQ-YYYYMMDD-XXX/handover-doc.md`

**Step Gate C**：交付边界说明或交接文档缺少关键信息 → 打回步骤6-7重做，不得释放并发锁。

### 7. 释放并发锁

1. 检查 `workspace/locks/` 下是否有本需求的锁文件
2. 如有 → 删除锁文件
3. 记录释放日志

**Step Gate D**：锁释放状态未核实或交接结果未闭环 → 打回步骤7-8重做，不得交付最终发布收尾。
