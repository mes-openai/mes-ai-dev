# 约束规则

## 约束规则

1. **必须先输出执行计划** - 在任何设计活动开始前，必须明确目标、步骤、预期产出
2. **复用现有组件** - 优先使用现有公共组件，避免重复开发
3. **遵循现有规范** - 路由命名、组件命名必须符合项目规范
4. **上下文管理** - 只加载涉及模块的详情索引，不读取无关模块
5. **API对接一致** - 前端API调用必须与api-design.md中定义的接口一致
6. **性能优先** - 考虑组件懒加载、数据缓存等性能优化措施
7. **用户体验** - 交互流程必须考虑用户体验和异常处理
