# 约束规则

## 约束规则

1. **必须先输出执行计划** - 在任何设计活动开始前，必须明确目标、步骤、预期产出
2. **遵循RESTful规范** - 所有接口设计必须符合RESTful最佳实践
3. **风格一致性** - 新接口必须与现有接口风格保持一致
4. **统一错误处理** - 使用统一的错误码体系和错误响应格式
5. **版本控制** - 接口必须包含版本号，便于后续升级
6. **向后兼容** - 接口变更必须考虑向后兼容性
7. **文档完整性** - 每个接口必须包含完整的请求/响应示例
