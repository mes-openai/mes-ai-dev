{
  "skill": "mes-refresh-update-dependency",
  "version": "1.0.0",
  "samples": [
    {
      "id": "trigger-001",
      "input": "Update dependency graph based on detected changes. Trigger: update dependency, refresh dependency graph, service dependency",
      "expected": "mes-refresh-update-dependency",
      "notes": "基础触发样例，后续可按真实场景扩充。"
    }
  ]
}
