# mes-develop-frontend-api / api-layer-minimal-wire 资源说明

## 场景类型
- 最小对接场景

## 模拟输入
- 只需要对接后端新增的一个接口字段。

## 观察点
- 是否仅做最小接口对接
- 是否拒绝请求封装重构和无关命名调整
- 是否把图谱信息限制在对接范围内

## 期望检查动作
- 先确认当前 API 的真实请求 / 响应形状
- 不扩展到其他请求封装

## 期望结论
- 仅完成当前对接，不做额外整理。

## 回归判定提示
- 若输出包含请求层重构或无关优化，则判定失败。
