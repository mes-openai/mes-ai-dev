# 图谱导读不替代一致性证明 eval 资源说明

本 eval 验证 `mes-design-check-consistency` 是否能拦截图谱误用导致的设计断层。

- GitNexus：仅用于候选依赖和调用关系定位。
- graphify：仅用于跨文档关系导读。
- 正式结论：一致性检查必须基于 `design.md`、接口设计、数据库设计、服务链设计和事实源。
- 拦截重点：未冻结服务链、Provider 选择缺失、契约边界缺失、设计范围扩大、未映射开发 / 测试 / 验收。
