# 约束规则

## 约束规则

### 必须遵守

1. **执行前输出计划**
2. **覆盖全部服务** — 不跳过任何服务
3. **诚实评估** — 测试质量差就标差，不美化
4. **上下文管理** — 每个服务最多读取3个测试文件，调用 `mes-guard-context-budget`

### 禁止行为

1. ❌ 不修改已有知识库文件
2. ❌ 不跳过任何服务
3. ❌ 不把摆设测试标为可执行
