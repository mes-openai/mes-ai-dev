{
  "skill": "mes-test-generate-unit",
  "version": "1.0.0",
  "samples": [
    {
      "id": "trigger-001",
      "input": "Generate unit test code for Java classes. Trigger: unit test, JUnit, mock test, Java test",
      "expected": "mes-test-generate-unit",
      "notes": "基础触发样例，后续可按真实场景扩充。"
    }
  ]
}
