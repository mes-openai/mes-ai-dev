# 数据库设计模板

> 本模板用于记录单个数据库表的完整设计信息。
> 使用说明：将 `[请填写xxx]` 替换为实际内容。

---

## 表基本信息

| 属性 | 值 |
|------|-----|
| 表名 | [请填写表名，如：sys_user] |
| 表类型 | [请填写：现有表 / 修改表 / 新增表] |
| Schema | [请填写Schema名称] |
| 表用途 | [请填写表用途描述] |
| 关联实体 | [请填写关联的Entity类名] |
| 表状态 | [请填写状态：新增/修改/删除] |
| 事实来源 | [请填写：DDL / database-index / database-registry / Entity / 现有SQL / 已批准新增] |
| 证据路径 | [请填写具体文件路径，至少1条] |
| 新增批准依据 | [若为新增表则必填；否则写“不适用”] |
| 创建日期 | [请填写创建或修改日期] |
| 设计人 | [请填写设计人姓名] |

---

## 表真实性核验

> 本节用于证明该表不是臆造对象，而是有真实数据库依据或明确新增批准依据。

| 核验项 | 结果 |
|--------|------|
| 是否可在真实数据库对象中找到该表 | [是/否/不适用] |
| 若为现有表，证据来自哪里 | [DDL / database-index / Entity / 现有SQL] |
| 若为新增表，是否已有明确批准依据 | [是/否/不适用] |
| 字段定义是否已与真实对象或数据字典对齐 | [是/否] |
| 结论 | [允许设计 / 阻断] |

### 证据摘录

- 证据1：[文件路径 + 说明]
- 证据2：[文件路径 + 说明]

> 若本节无法填完整，本表不应进入正式数据库设计文档。

---

## 数据变更风险分级

> 对本次数据库变更进行风险分级，确定执行方案和审批要求。

| 风险维度 | 评估项 | 评估结果 |
|---------|--------|---------|
| 变更类型 | 新增表 / 新增字段 / 修改字段类型 / 删除字段 / 删除表 / 修改索引 | [请填写] |
| 数据量级 | 目标表现有数据量（新增表填0） | [请填写行数] |
| 业务关键度 | 核心业务表 / 重要业务表 / 辅助表 / 配置表 | [请填写] |
| 关联影响 | 被关联的表数 | [请填写数量] |
| 是否涉及数据迁移 | 是 / 否 | [请填写] |
| 是否涉及停机 | 是 / 否 | [请填写] |

### 风险等级判定

| 风险等级 | 判定条件 | 审批要求 | 执行要求 |
|---------|---------|---------|---------|
| 🟢 低风险 | 新增表 / 新增可空字段 / 配置表变更 | 开发自审通过即可 | 可在业务时段执行 |
| 🟡 中风险 | 新增必填字段（有默认值） / 新增索引 / 修改字段长度 | 技术负责人审批 | 建议在低峰时段执行 |
| 🔴 高风险 | 修改字段类型 / 删除字段 / 删除表 / 涉及数据迁移 / 核心业务表变更 | 技术负责人 + DBA审批 | 必须在维护窗口执行，须有回滚脚本 |

**本次变更风险等级**：🟢 低风险 / 🟡 中风险 / 🔴 高风险

**判定理由**：[请填写判定理由]

---

## 字段设计

### 字段列表

| 字段名 | 数据类型 | 长度 | 是否必填 | 默认值 | 主键 | 自增 | 说明 |
|--------|----------|------|----------|--------|------|------|------|
| [请填写字段名] | [请填写类型] | [请填写长度] | 是/否 | [请填写默认值] | 是/否 | 是/否 | [请填写说明] |

### 标准字段（建议包含）

| 字段名 | 数据类型 | 长度 | 是否必填 | 默认值 | 说明 |
|--------|----------|------|----------|--------|------|
| id | bigint | - | 是 | 自增 | 主键ID |
| create_time | datetime | - | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | datetime | - | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| create_by | varchar | 50 | 否 | - | 创建人 |
| update_by | varchar | 50 | 否 | - | 更新人 |
| deleted | tinyint | - | 否 | 0 | 删除标记（逻辑删除） |
| version | int | - | 否 | 0 | 版本号（乐观锁） |

---

### 字段详细说明

#### 字段1: [请填写字段名]

| 属性 | 值 |
|------|-----|
| 字段名 | [请填写字段名] |
| 数据类型 | [请填写数据类型] |
| 长度 | [请填写长度] |
| 是否必填 | [请填写是否必填] |
| 默认值 | [请填写默认值] |
| 校验规则 | [请填写校验规则] |
| 业务含义 | [请填写业务含义] |
| 取值范围 | [请填写取值范围] |
| 示例值 | [请填写示例值] |

---

#### 字段2: [请填写字段名]

[同上格式]

---

## 索引设计

### 索引列表

| 索引名 | 索引字段 | 索引类型 | 是否唯一 | 说明 |
|--------|----------|----------|----------|------|
| [请填写索引名] | [请填写字段名] | 普通/唯一/全文 | 是/否 | [请填写说明] |

### 标准索引（建议包含）

| 索引名 | 索引字段 | 索引类型 | 是否唯一 | 说明 |
|--------|----------|----------|----------|------|
| PRIMARY | id | 主键 | 是 | 主键索引 |
| idx_create_time | create_time | 普通 | 否 | 创建时间索引（便于按时间查询） |
| idx_deleted | deleted | 普通 | 否 | 删除标记索引（便于过滤已删除数据） |

---

### 索引详细说明

#### 索引1: [请填写索引名]

| 属性 | 值 |
|------|-----|
| 索引名 | [请填写索引名] |
| 索引字段 | [请填写字段名] |
| 索引类型 | [请填写类型] |
| 是否唯一 | [请填写是否唯一] |
| 设计理由 | [请填写设计理由，如：该字段常用于查询条件] |
| 预期收益 | [请填写预期收益，如：提升查询性能] |

---

## 关联关系

### 表关联设计

| 关联表 | 关联字段 | 本表字段 | 关联类型 | 说明 |
|--------|----------|----------|----------|------|
| [请填写关联表名] | [请填写关联表字段] | [请填写本表字段] | 一对一/一对多/多对多 | [请填写说明] |

示例：

| 关联表 | 关联字段 | 本表字段 | 关联类型 | 说明 |
|--------|----------|----------|----------|------|
| sys_role | id | role_id | 一对多 | 一个用户可有多个角色 |
| sys_department | id | dept_id | 一对一 | 一个用户属于一个部门 |

---

### 外键约束（可选）

| 外键名 | 本表字段 | 关联表 | 关联字段 | 约束类型 | 说明 |
|--------|----------|--------|----------|----------|------|
| [请填写外键名] | [请填写本表字段] | [请填写关联表] | [请填写关联字段] | [请填写约束类型] | [请填写说明] |

---

## DDL脚本

### 创建表脚本

```sql
-- 创建表 [请填写表名]
CREATE TABLE [请填写表名] (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `create_by` VARCHAR(50) DEFAULT NULL COMMENT '创建人',
    `update_by` VARCHAR(50) DEFAULT NULL COMMENT '更新人',
    `deleted` TINYINT DEFAULT 0 COMMENT '删除标记',
    `version` INT DEFAULT 0 COMMENT '版本号',
    
    -- [请填写业务字段]
    `[请填写字段名]` [请填写类型]([请填写长度]) [NOT NULL] [DEFAULT [请填写默认值]] COMMENT '[请填写说明]',
    
    PRIMARY KEY (`id`),
    -- [请填写索引]
    INDEX `idx_create_time` (`create_time`),
    INDEX `idx_deleted` (`deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='[请填写表用途说明]';
```

---

### 修改表脚本

```sql
-- 新增字段
ALTER TABLE [请填写表名] 
ADD COLUMN `[请填写字段名]` [请填写类型]([请填写长度]) [NOT NULL] [DEFAULT [请填写默认值]] COMMENT '[请填写说明]';

-- 修改字段
ALTER TABLE [请填写表名] 
MODIFY COLUMN `[请填写字段名]` [请填写新类型]([请填写新长度]) COMMENT '[请填写说明]';

-- 新增索引
ALTER TABLE [请填写表名] 
ADD INDEX `[请填写索引名]` (`[请填写字段名]`);

-- 删除字段
ALTER TABLE [请填写表名] 
DROP COLUMN `[请填写字段名]`;
```

---

## 数据迁移方案

### 迁移场景

| 迁移类型 | 说明 |
|----------|------|
| [请填写类型：初始化数据/存量数据迁移/数据同步] | [请填写说明] |

---

### 初始化数据脚本

```sql
-- 初始化数据
INSERT INTO [请填写表名] (
    [请填写字段列表]
) VALUES (
    [请填写值列表]
);

-- 批量初始化
INSERT INTO [请填写表名] ([请填写字段列表]) VALUES
([请填写值1]),
([请填写值2]),
([请填写值3]);
```

---

### 存量数据迁移脚本

```sql
-- 存量数据迁移
-- 从旧表迁移到新表
INSERT INTO [请填写新表名] (
    [请填写字段列表]
)
SELECT 
    [请填写源字段列表]
FROM [请填写旧表名]
WHERE [请填写条件];

-- 数据清洗
UPDATE [请填写表名]
SET [请填写字段] = [请填写值]
WHERE [请填写条件];
```

---

## 变更历史

| 版本 | 变更日期 | 变更类型 | 变更内容 | 变更人 |
|------|----------|----------|----------|--------|
| v1.0 | [请填写日期] | 新增 | [请填写变更内容] | [请填写变更人] |
| v1.1 | [请填写日期] | 修改 | [请填写变更内容] | [请填写变更人] |

---

## 备注

### 设计注意事项

[请填写设计时的注意事项，如：]
- 本表数据量预估：约XX万条
- 查询热点：按XX字段查询
- 数据增长速度：每日新增约XX条

### 相关文档

| 文档类型 | 文档名称 | 说明 |
|----------|----------|------|
| 需求文档 | [请填写文档名] | [请填写说明] |
| 设计文档 | [请填写文档名] | [请填写说明] |

### 真实性校验补充说明

- 若该表是现有表，请说明其在真实数据库对象中的定位方式。
- 若该表是新增表，请说明为什么现有表不能复用，以及新增审批结论来自哪里。

---

## Entity类定义参考

```java
/**
 * [请填写实体类用途说明]
 */
@Entity
@Table(name = "[请填写表名]")
public class [请填写Entity类名] {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "[请填写字段名]", nullable = false)
    private [请填写类型] [请填写字段名];

    // 其他字段...

    @Column(name = "create_time", nullable = false)
    private Date createTime;

    @Column(name = "update_time", nullable = false)
    private Date updateTime;

    @Column(name = "create_by")
    private String createBy;

    @Column(name = "update_by")
    private String updateBy;

    @Column(name = "deleted")
    private Integer deleted;

    @Version
    @Column(name = "version")
    private Integer version;

    // getter/setter方法...
}
```
